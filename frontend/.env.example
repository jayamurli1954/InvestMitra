# InvestMitra frontend — copy to .env to override defaults
#
# Must match backend BACKEND_PORT in backend/.env
REACT_APP_API_URL=http://127.0.0.1:9001

# Optional: override WebSocket URL (defaults to ws:// same host:port as API)
# REACT_APP_WS_URL=ws://127.0.0.1:9001
